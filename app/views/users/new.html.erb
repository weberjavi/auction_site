<h2>Create a new User</h2>

<%= form_for [@user] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <br>
  <%= f.label :email %>
  <%= f.text_field :email %>

  <%= f.submit "save" %>
<% end %>