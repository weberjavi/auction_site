<h1>Users list</h1>

<ul>
  <% @users.each do |user|%>
    <li>
      name: <%= user.name %> // email: <%= user.email %>
      <%= link_to("Add product", new_user_product_path(user.id))%>
    </li>
  <% end %>
</ul>